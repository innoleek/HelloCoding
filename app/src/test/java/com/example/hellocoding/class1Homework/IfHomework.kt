package com.example.hellocoding.class1Homework

import org.junit.Test

class IfHomework {

    @Test
    fun `task 1`() {
        /*
         Write a program so that this function prints "Positive Number", "Negative Number" or
         "Zero" given any input

         For example, if "input" is 100, function should print "Positive Number"
         If "input" is -100, function should print "Negative Number"
         */
        var input = 2837475

        if (input > 2837475) {
            print("Negative Number")
        }
    }
}